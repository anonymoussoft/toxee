<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- H11 (2026-05-19 review): READ_EXTERNAL_STORAGE is deprecated on API 33+
         and is split into per-media-type permissions. Cap the legacy permission
         at SDK 32 and declare the granular replacements alongside it so that
         requests work on every supported SDK level. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
                     android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
                     android:maxSdkVersion="28"/>
    <!-- Android 13+ requires runtime POST_NOTIFICATIONS permission for any
         app that posts user-visible notifications. flutter_local_notifications
         requests this on-demand from the plugin's Android impl, but the
         manifest entry is mandatory. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Explicit INTERNET permission. Normally implied by Flutter's manifest
         merge, but declared here so the requirement is visible and won't
         silently break if the merge order changes. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Foreground service permissions for ToxPollingService. The service
         keeps the Flutter engine and tox polling loop alive while the app is
         in the background. FOREGROUND_SERVICE is required on Android 9+ for
         any foreground service; FOREGROUND_SERVICE_DATA_SYNC and
         FOREGROUND_SERVICE_PHONE_CALL are the granular subtype permissions
         introduced in Android 14, each matching a value declared on the
         <service android:foregroundServiceType=...> below. Holding only
         FOREGROUND_SERVICE without the right subtype causes
         ForegroundServiceTypeException on API 34+. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL"/>
    <!-- Android requires a phoneCall foreground service to either declare
         MANAGE_OWN_CALLS or run as the default dialer. Toxee is a VoIP app, not
         the device dialer, so declare the self-managed-call prerequisite. -->
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS"/>
    <!-- Needed to launch full-screen incoming-call UI from a notification when
         the device is locked. Without it Android 14+ silently downgrades the
         full-screen intent to a heads-up. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <application
        android:label="Toxee"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- ToxPollingService: persistent foreground service that keeps the
             Flutter engine alive so FfiChatService.startPolling() continues to
             drain tox events while the app is backgrounded. The combined
             foregroundServiceType lets the service transition between the
             dataSync (default) and phoneCall (active call) modes at runtime
             without re-registering. exported=false because the service is
             driven exclusively from in-process Dart code via
             RuntimeForegroundChannel. -->
        <service
            android:name=".ToxPollingService"
            android:exported="false"
            android:foregroundServiceType="dataSync|phoneCall" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
