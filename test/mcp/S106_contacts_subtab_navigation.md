# S106 — Contacts: sub-tab navigation (New Contacts / Group Notifications / Blocked Users)

**Layer**: L3 (MCP playbook)
**Fixture vector**: `accounts=1 current=A1 autoLogin=on network=any window=default friends=any`
**Harness mode**: peerHarness=none — the three sub-tabs and the panels they open are single-instance and hermetically renderable; no second Tox endpoint is required to drive navigation. (`contact_application_anchors_test.dart` proves the three tab keys render under `TestWidgetsFlutterBinding`.)
**Promotion target**: L1 WidgetTester (surface proven renderable — the existing `test/ui/contact/contact_application_anchors_test.dart` already finds all three tab keys in a `_buildTabHarness()`; this scenario is the L3 mirror that also asserts the panel mounts on real taps). No live-DHT dependency.
**Status**: covered (L1 WidgetTester real-UI gate — `test/ui/contact/contact_subtab_navigation_real_ui_test.dart`). The gate pumps the REAL `TencentCloudChatContact` component forced to the desktop layout and proves the desktop **pane-swap navigation**: a real tap on each keyed sub-tab mounts its panel (New Contacts → `TencentCloudChatContactApplication`; Blocked Users → `TencentCloudChatContactBlockList`) and updates the pane title (`tL10n.newContacts` / `tL10n.blockList`) via `setState`. **Scope correction vs. this spec's title:** toxee's `TencentCloudChatContact.desktopBuilder` ships only TWO sub-tabs — `new_contacts` and `blocked_users`; the upstream "Group Notifications" tab (`contact_group_notifications_tab` / `TencentCloudChatContactGroupApplicationList`) was deliberately removed (`tencent_cloud_chat_contact.dart:197-203` — the Tox bridge has no group-application concept), so the gate asserts that key is **absent** on the live component. A3 (Group Notifications panel) is therefore not exercisable on the real toxee UI by design. Live-DHT content (real application/block rows) stays L3. The pre-existing `contact_application_anchors_test.dart` remains the static anchor-key check; its "all three keys" only holds because it hand-fabricates a `group_notification` tab item production never builds.
**Covered-by**: `test/ui/contact/contact_subtab_navigation_real_ui_test.dart`
**Mobile parity**: the sub-tabs (`TencentCloudChatContactTab`) and the panels (`TencentCloudChatContactApplication` / `TencentCloudChatContactBlockList`) are shared UIKit-fork widgets reused on every platform. The L1 gate exercises the **desktop** arm (the `desktopBuilder` in-place pane swap). The **mobile** arm of the same parent shares the identical tab `onTap` wiring but `Navigator.push`-es a full route per tab (`tencent_cloud_chat_contact.dart` `defaultBuilder` ~256-296) and exposes `new_contacts` + `groups` (My Group) + `blocked_users` — note mobile also has NO `group_notification` tab. Driving the mobile route-push arm in a WidgetTester needs a registered route stack; it is left to the L3 device/marionette run (and tracked here) — the underlying tab keys + panel widgets are the same Dart, so the shared wiring is already covered.

## Precondition
- One account A logged in, plaintext, sidebar Online (poll `<nick>\nOnline` ≤60s) — `friends`/`blockedUsers`/`friendApplications` state is "don't care" for navigation; the tabs render regardless of contents.
- Debug bundle built with `--dart-define=MCP_BINDING=marionette --dart-define=TOXEE_L3_TEST=true`; launched `MCP_BINDING=marionette ./run_toxee.sh`.
- Desktop vs mobile path differ and must be distinguished when asserting (see Notes): desktop `desktopBuilder` swaps the right pane via `setState` (`tencent_cloud_chat_contact.dart:189-221`); mobile `defaultBuilder` `Navigator.push`-es a new route per tab (`tencent_cloud_chat_contact.dart:269-311`).

## UI Driver
1. `marionette.tap(UiKeys.sidebarContacts)` (`sidebar_contacts_tab`)
2. `marionette.tap({ key: "contact_new_contacts_tab" })` via `UiKeys.contactNewContactsTab` (`contact_new_contacts_tab`); wait ~500ms for the route/pane swap
3. `fmt_semantic_snapshot` → capture the New-Contacts panel (`TencentCloudChatContactApplication`) — record label `S106_new_contacts_panel`
4. On mobile: `marionette.press_back_button` to pop the route; on desktop: skip (pane is swapped in place)
5. `marionette.tap({ key: "contact_group_notifications_tab" })` via `UiKeys.contactGroupNotificationsTab` (`contact_group_notifications_tab`); wait ~500ms
6. `fmt_semantic_snapshot` → capture the Group-Notifications panel (`TencentCloudChatContactGroupApplicationList`) — record label `S106_group_notifications_panel`
7. On mobile: `marionette.press_back_button`; on desktop: skip
8. `marionette.tap({ key: "contact_blocked_users_tab" })` via `UiKeys.contactBlockedUsersTab` (`contact_blocked_users_tab`); wait ~500ms
9. `fmt_semantic_snapshot` → capture the Blocked-Users panel (`TencentCloudChatContactBlockList`) — record label `S106_blocked_users_panel`

> **L1 coverage map** (`contact_subtab_navigation_real_ui_test.dart`): A1 (corrected: the two real toxee tabs present + Group-Notifications key/panel ASSERTED ABSENT) + A2 (New-Contacts panel mounts on tap, pane title `tL10n.newContacts`) + A4 (Blocked-Users panel mounts on tap, pane title `tL10n.blockList`, empty state `tL10n.noBlockList`) are all proven at L1, plus a re-swap test (tab→tab→tab) proving the live pane state machine. A3 is **not exercisable** (tab removed in toxee). A5/A6 (live `blockedUsers[]` cross-check, runtime-error baseline) remain L3 — they need the running app + native bridge.

## Assertions
- A1 (toxee-corrected, L1): the **two** real sub-tab keys are findable on the live `TencentCloudChatContact` — `contact_new_contacts_tab`, `contact_blocked_users_tab`. `contact_group_notifications_tab` is asserted **absent** (toxee removed the tab, `tencent_cloud_chat_contact.dart:197-203`). (The synthetic `contact_application_anchors_test.dart:60-61` still asserts the upstream trio from a hand-built harness.)
- A2 (L1): after tapping New Contacts, the New-Contacts panel mounts — `find.byType(TencentCloudChatContactApplication)` present AND the pane title `tL10n.newContacts` (desktop sets `_title = tL10n.newContacts`, `tencent_cloud_chat_contact.dart:191`). (Application rows `contact_application_item:<userId>` / `contact_applications_list_empty` reflect live `friendApplications[]` → L3.)
- A3 (NOT APPLICABLE on toxee): the Group-Notifications panel/tab was removed; `TencentCloudChatContactGroupApplicationList` is never mounted by the toxee Contacts component.
- A4 (L1): after tapping Blocked Users, the Blocked-Users panel mounts — `find.byType(TencentCloudChatContactBlockList)` present, pane title `tL10n.blockList` (`tencent_cloud_chat_contact.dart:212`), empty list shows `tL10n.noBlockList`.
- A5 (L3): `l3_dump_state` `blockedUsers[]` count equals the number of rows rendered in the Blocked-Users snapshot (cross-checks the panel reflects real state, `l3_debug_tools.dart:3598`)
- A6 (L3): `official.get_runtime_errors({})` empty vs the Step-0 baseline

## Notes
- L1 DONE (desktop pane-swap): `test/ui/contact/contact_subtab_navigation_real_ui_test.dart` pumps the parent `TencentCloudChatContact` forced to desktop, taps each keyed tab, and asserts the panel widget mounts + the pane title swaps (the real `setState` navigation, not just key presence). The harness wiring needed: `deviceScreenType = desktop`, `usedComponents = [contact]`, a non-null `contactBuilder`, and the `_localized()` `tL10n` init (all reset in tearDown). Remaining L3: live application/block rows, the `blockedUsers[]` cross-check, runtime-error baseline, and the **mobile route-push arm** (needs a registered route stack).
- L3-pin reason (residual): live content + the mobile `Navigator.push` route arm. The desktop navigation surface is now L1.
- Keys verified: `contact_new_contacts_tab`/`contact_group_notifications_tab`/`contact_blocked_users_tab` at `tencent_cloud_chat_contact_tab.dart:46-49` (mobile `defaultBuilder`, GestureDetector) AND `:110-113` (desktop `desktopBuilder`, InkWell) — `switch (widget.item.id)` keys the wrapper per tab id. NOTE: toxee's `TencentCloudChatContact` only ever feeds `new_contacts` + `blocked_users` (desktop) / `new_contacts` + `groups` + `blocked_users` (mobile), so `contact_group_notifications_tab` is dead in production — the key exists in the fork switch but no toxee tab item triggers it.
- Sibling distinction: S26/S27 tap `contact_new_contacts_tab` only as a step toward accept/decline; S106 is the dedicated navigation test that asserts ALL THREE tabs + their panels. S107 drills into the Blocked-Users tab specifically; S109 into the New-Contacts empty state; S110 into Group-Notifications.
- Gotcha: the mobile path pushes a full route per tab (must `press_back_button` between tabs); the desktop path swaps `_desktopModule` in place (no back). Drive the matching arm for the platform under test.
- Tab wrappers are `GestureDetector`/`InkWell` with `onTap` (no `Semantics.onTap`) → `fmt_tap_widget` may no-op; use marionette key tap. Wait ~500ms after each tap for the route/pane animation (F14, `doc/research/UI_TEST_RUN_FINDINGS.en.md`).
