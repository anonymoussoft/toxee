# S37 — Group member moderation (kick / role change)

**Layer**: L3 (MCP playbook)
**Fixture vector**: `paired_for_e2e` = `accounts=2(A=owner,B=member in separate sandboxes) current(A)=A1 current(B)=B1 autoLogin=on network=online groups=[gidG, A owner, B member]`
**Harness mode**: peerHarness=none (would need two real toxees)
**Promotion target**: n/a — the kick/remove path is live but stays L3-pinned on two real toxees plus NGC delivery/removal behavior; the role-change variant remains unwired.
**Status**: ✅ RESOLVED 2026-06-02 — S37 group-kick PASSES the live two-process gate (`run_fixture_c_kick.sh`: A1 join + A2 kick-resolve/send + A3 B-removed). The three successive blockers were all fixed: NGC identity model (member-list tool), NGC peer connectivity (full-mesh local bootstrap), and B-side self-kick removal (HandleGroupModeration native fix). Historical narrative follows. kick reaches C++ (Dart fixes landed); was blocked on NGC identity model. FIXED: the Platform no-op (kickGroupMember -> C++ tox_group_kick_peer), the 76->64 pubkey normalize, and KickGroupMember now resolves the peer via the callback-populated group_peer_id_cache_ first (codex-approved; build_ffi.sh-verified). ROOT CAUSE (live-confirmed in A's log): the founder's HandleGroupPeerJoin DOES fire, but caches the joiner under its GROUP-SPECIFIC public key (e.g. 6C6173B6...), which is DIFFERENT from the joiner's Tox/friend public key (A04B987F... = toxB). NGC group members are identified by a per-group key, not the friend key, so 'kick member by friend userID' can never resolve. Gate run_fixture_c_kick.sh (A1 join + A2 kick-send PASS; A3 removal blocked). LANDED (session 7): the group MEMBER LIST is now exposed via the new `l3_group_member_list` tool (`getGroupManager().getGroupMemberList` → C++ `GetGroupMemberList`; each REMOTE member's `userID` IS its NGC group pubkey, built from the SAME `group_peer_id_cache_` `KickGroupMember` consults — `V2TIMGroupManagerImpl.cpp:1835`/`:1879`), and the kick driver now RESOLVES B's group identity from A's member list (polls `l3_group_member_list` until a non-self member appears — which also proves A's peer cache holds B) and kicks by THAT identity instead of toxB; it also now PRIMES A's peer cache via a B→A group message (HandleGroupMessageGroup caches the sender, `V2TIMManagerImpl.cpp:4893`) because the founder's HandleGroupPeerJoin proved unreliable. Analyzer-clean. **LIVE (session 7, 3 kick runs + 1 S34 control on `paired_for_e2e`):** A1 (join) PASSES reliably, but the kick is blocked ONE LAYER DEEPER than the identity format — the two instances never establish NGC group PEER connectivity. A's `HandleGroupPeerJoin` never fires (cache=0 peers for 90s despite is_connected=1) AND group messages don't roundtrip in EITHER direction; the PROVEN S34 group-message gate ALSO fails identically this session (B never receives A's group msg) while C2C friend connectivity works (connection=2/UDP). So A's peer cache stays empty and the kick has no peer to resolve. (This SUPERSEDES the earlier "HandleGroupPeerJoin DOES fire" note above — that was a luckier run.) The cache-priming path works once peers connect. **UPDATE (session 7 cont.) — connectivity FIXED; A1 + A2 now PASS live.** Root-caused the no-connectivity: same-host instances bootstrap to the PUBLIC DHT but never to EACH OTHER, so public-group peer discovery never converges (the tim2tox auto_tests fix this with a full-mesh local bootstrap — their comment names this exact "peer_join never fires on founder" symptom). FIX: new L3 tools `l3_dht_info` + `l3_add_bootstrap_node`; the kick driver now full-mesh loopback-bootstraps A↔B before the group flow. With that, LIVE: A1 join PASS, B→A group msg roundtrips, **A resolves B's NGC group identity `891964B5…` (≠ friend toxB `A04B987F…`) and the kick SUCCEEDS** (`KickGroupMember: Successfully kicked … peer_id=1`, code=0) → **A2 PASS**. Only **A3 (B-side removal)** remains: B RECEIVES the kick (`HandleGroupModeration … target_peer=self mod_type=KICK` fires on B) but does NOT drop the group from `knownGroups` — the C++ KICK case (`V2TIMManagerImpl.cpp:6943`) is a no-op for the self-target, and the Platform-path `FfiChatService` has no kicked-event subscription to remove from `_knownGroups`. **A3 FIXED + LIVE-PASSING (2026-06-02).** Native fix in `HandleGroupModeration` (`V2TIMManagerImpl.cpp:6943`): on a self-target KICK, erase native group state (`EraseGroupLocalState` — group_number/chat-id maps + peer cache) and call `DartNotifyGroupQuit(groupID)`, which posts `groupQuitNotification` → `Tim2ToxSdkPlatform` → `FfiChatService.cleanupGroupState` → removes the group from `_knownGroups`. Also suppressed the self-target `OnMemberInfoChanged` for KICK (a kick isn't a role-change). **codex-reviewed the plan (verdict needs-change → both findings applied: native-state cleanup + OnMemberInfoChanged suppression); build_ffi.sh-verified; run_fixture_c_kick.sh now PASSES A1+A2+A3** (kick resolves B's group id `26FAEE8A…`, code=0, B's knownGroups drops the group on round 1). Role-change is a separate unwired path.

> Historical note: the long narrative below preserves the debugging path. Any
> later references to "informational only" or "Platform no-op" are superseded
> for the **kick/remove** path; only the role-change variant remains unwired.

## Precondition
- A owner / B member of `<gidG>`; both online; two sandboxes (distinct `CFBundleIdentifier`).
- Kick/remove is runnable via the live two-process gate; the role-change branch remains record-only.

## Driver
1. A: group `<gidG>` → profile → member list → tap B's row → `onManageMember` sheet (S36 driver).
2. A: tap Kick/Remove via `UiKeys.groupMemberActionKickButton` (`group_member_action_kick_button`) OR role change (`:210` `setGroupMemberRole`).

## Assertions (kick path live; role variant still unwired)
- A1: A log `KickGroupMember: Successfully kicked member {} (peer_id={})` (`V2TIMGroupManagerImpl.cpp:2989`).
- A2: B receives a KICK GroupTips (tip_type=3) via `NotifyOtherInstancesMemberKicked` and drops `<gidG>` membership.
- A3: role variant remains unwired — `SetGroupMemberRole` still does not produce the equivalent two-process effect from the UI path today.
- A4: A's member-list row count drops by one (cross-check `loadGroupMemberList`, S36 idiom).

## Notes
- Current state: the kick/remove path is live and covered by `run_fixture_c_kick.sh`; the historical no-op narrative below is preserved only for debugging context. Role-change is the remaining unwired branch.
- Core claim: UIKit routes kick/role through `getGroupManager()` → `third_party/tencent_cloud_chat_sdk/lib/manager/v2_tim_group_manager.dart` → installed Platform (`kickGroupMember`:399→Platform:406, `setGroupMemberRole`:435→Platform:442). On toxee that is `Tim2ToxSdkPlatform`, where both are stubs: `kickGroupMember` (`third_party/tim2tox/dart/lib/sdk/tim2tox_sdk_platform.dart:8868`) allocates FFI args but never calls them — only fires local `onMemberKicked` (`:8917-8927`), returns success; `setGroupMemberRole` (`:8946`) is a pure no-op (`:8951`). `muteGroupMember`:8762/`transferGroupOwner`:8960 likewise stubbed.
- Real Tox moderation EXISTS in C++ but on the **binary-replacement** path UIKit does not use here: `KickGroupMember`→`tox_group_kick_peer` (`V2TIMGroupManagerImpl.cpp:2869`/`:2984`), `SetGroupMemberRole`→`tox_group_set_role` (`:3057`/`:3160`); C FFI `DartKickGroupMember`/`DartModifyGroupMemberInfo` (`third_party/tim2tox/ffi/dart_compat_group.cpp:1475`/`:486`) + cross-instance `NotifyOtherInstancesMemberKicked` (`:323`). The native_im adapter `third_party/tencent_cloud_chat_sdk/lib/native_im/adapter/tim_group_manager.dart:567`/`:597` calls them — but that is binary-replacement, not the Platform route UIKit invokes.
- Net: from the UI an MCP test drives, kick = local-only callback, role = no-op. No observable two-process effect → `informational only`, NOT merely Fixture-C-blocked (even two real toxees, the stub no-ops on the wire). `MarkGroupMemberList` is `ERR_SDK_INTERFACE_NOT_SUPPORT` (`:3176`) even in C++.
- Sibling: S36 (`test/mcp/S36_group_member_list.md`) covers the member-list read surface A4 reuses. If the stubs are ever wired this flips to `blocked on Fixture C spike` (`doc/research/MULTI_INSTANCE_SPIKE.en.md`).
